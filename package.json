{
  "name": "tournament-scheduler",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild index.jsx --bundle --outfile=bundle.js --format=iife --platform=browser --jsx=automatic --minify"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
